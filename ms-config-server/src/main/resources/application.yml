server:
  port: 7070




spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MathiuZuri/aplicaiones-distribuidas # ruta del repositorio git
          searchPaths: config-data
          default-label: main
